// `Buffer Slots Primitives` is the package umbrella ([MOD-005]).
//
// Slots is single-variant and `Buffer.Slots` carries no Copyable-imposing conformance, so this
// is the degenerate type/ops split: an exports-only umbrella over the type module.
@_exported public import Buffer_Slots_Primitive
